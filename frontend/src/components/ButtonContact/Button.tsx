import { FaEdit } from 'react-icons/fa';
import { FaTrash } from 'react-icons/fa';

function Buttons () {
    return (
        <div>
            <FaEdit
                className='edit-icon ' 
                onClick={() => ({})}
            />
            <FaTrash
                className='edit-icon ' 
                onClick={() => ({})}
        />
        </div>

    );

}

export default Buttons;